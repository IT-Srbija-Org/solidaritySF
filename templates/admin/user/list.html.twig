{% extends 'admin/base.html.twig' %}

{% block content %}
    {{ include('flashes.html.twig') }}

    {% form_theme form with ['form.html.twig', 'form_inline.html.twig'] %}
    {{ form(form) }}

    <div class="overflow-x-auto mt-6">
        <div class="text-gray-500 mb-2">
            Ukupno rezultata: <span class="font-bold">{{ users.total }}</span>
        </div>

        <table class="table rounded-xl border border-base-200">
            <thead class="bg-gray-100">
                <tr>
                    <th>
                        {{ include(
                            'sort.html.twig',
                            { 'path': 'admin_user_list', 'sort': 'id', 'default_direction': 'desc', 'title': 'ID' }
                        ) }}
                    </th>
                    <th>
                        {{ include(
                            'sort.html.twig',
                            { 'path': 'admin_user_list', 'sort': 'fullName', 'title': 'Ime', 'title_icon': 'ti-user' }
                        ) }}
                    </th>
                    <th>
                        {{ include(
                            'sort.html.twig',
                            { 'path': 'admin_user_list', 'sort': 'role', 'title': 'Privilegija', 'title_icon': 'ti-military-rank' }
                        ) }}
                    </th>
                    <th>
                        {{ include(
                            'sort.html.twig',
                            { 'path': 'admin_user_list', 'sort': 'createdAt', 'title': 'Registrovan', 'title_icon': 'ti-calendar-event' }
                        ) }}
                    </th>
                    <th>
                        <span class="ti ti-calendar-event text-xl"></span>
                        Poslednji put viđen
                    </th>
                    <th>
                        {{ include(
                            'sort.html.twig',
                            { 'path': 'admin_user_list', 'sort': 'isActive', 'title': 'Aktivan?' }
                        ) }}
                    </th>
                    <th>Verifikovan?</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                {% for user in users.items %}
                    <tr class="hover:bg-gray-100">
                        <td>{{ user.id }}</td>
                        <td>
                            <div>{{ user.fullName }}</div>
                            <a href="{{ path('admin_user_details', { 'id': user.id }) }}" class="link link-primary link-hover">
                                {{ user.email }}
                            </a>
                        </td>
                        <td>{{ user.getRoles|roleTranslate }}</td>
                        <td class="whitespace-nowrap">
                            {{ user.createdAt|date('H:i d.m.Y.') }}
                        </td>
                        <td class="whitespace-nowrap">
                            {% if user.lastVisit %}
                                {{ user.lastVisit|date('H:i d.m.Y.') }}
                            {% else %}
                                -
                            {% endif %}
                        </td>
                        <td>
                            {% if user.isActive %}
                                <div class="status status-success mb-1"></div>
                                Da
                            {% else %}
                                <div class="status status-error mb-1"></div>
                                Ne
                            {% endif %}
                        </td>
                        <td>
                            {% if user.isEmailVerified %}
                                <div class="status status-success mb-1"></div>
                                Da
                            {% else %}
                                <div class="status status-error mb-1"></div>
                                Ne
                            {% endif %}
                        </td>
                        <td>
                            <div class="flex gap-2 justify-end">
                                <a href="{{ path('admin_user_edit', { 'id': user.id }) }}" class="btn btn-primary btn-sm flex tooltip" data-tip="Izmeni">
                                    <span class="ti ti-edit text-xl"></span>
                                </a>
                                <a href="{{ path('admin_user_details', { 'id': user.id }) }}" class="btn btn-primary btn-sm flex tooltip" data-tip="Detalji">
                                    <span class="ti ti-info-circle text-xl"></span>
                                </a>
                            </div>
                        </td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>

    {% import "macro.html.twig" as macro %}
    {{ macro.pagination('admin_user_list', users.total_pages, users.current_page) }}
{% endblock %}
